/**
 * Longest Consecutive Sequence
 * ============================
 * Given an unsorted array of integers, find the length of the
 * longest consecutive sequence of numbers.
 *
 * Must run in O(n) time.
 *
 * Examples:
 *   longestConsecutive([100, 4, 200, 1, 3, 2])  → 4   // [1, 2, 3, 4]
 *   longestConsecutive([0, 3, 7, 2, 5, 8, 4, 6, 0, 1]) → 9  // [0..8]
 *   longestConsecutive([])  → 0
 *
 * Constraints:
 *   - O(n) time — sorting is not acceptable here
 *   - Return only the length, not the sequence itself
 */

function longestConsecutive(nums) {
  // your code here
}

exports.solution = longestConsecutive;
